# Localization audit allowlist (issue #132).
#
# Each non-comment line suppresses a LocalizationAuditTests violation:
#   - "RelativePath.axaml" or "RelativePath.cs"  -> the WHOLE file is deferred (not yet migrated).
#   - "RelativePath|snippet"                     -> a single justified technical literal.
#
# Pass 2 (#132) migrated the editor views and ViewModels into the resource system, so this list is
# now near-empty. Any file that appears below is a documented, justified exception. New views/
# ViewModels are enforced by default (they must use {loc:Loc} / LocalizedStrings).
#
# ---- Justified exceptions (regenerated by the pass-2 audit) ----

# ---- Deferred views (auto-generated backlog; migrate incrementally) ----

# ---- Deferred ViewModels (auto-generated backlog) ----

# ---- Deferred: views/ViewModels introduced by PR #155 (backup manager / save compare) ----
